# # Math Chatbot (Multimodal-Ready)

A Streamlit app that acts like an Instruct-style math assistant:
- Chat math questions (SymPy backend)
- Infers intent to handle image/audio/PDF uploads (stubs provided)

## Local Run

```bash
pip install -r requirements.txt
streamlit run app.py
